/**
 * Smoke test to ensure the test harness runs in CI.
 * Replace with real integration tests as API routes land.
 */

describe('CI smoke test', () => {
  it('runs', () => {
    expect(1 + 1).toBe(2)
  })
})
